// append the users partial to our div with class of container
$('.evaluation-container').append('<%= escape_javascript(render(:partial => @evaluations)) %>');

<% if @evaluations.empty? %>
  $('.load-more').remove();
<% end %>